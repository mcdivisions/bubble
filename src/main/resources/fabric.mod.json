{
  "schemaVersion": 1,
  "id": "bubble",
  "name": "Bubble",
  "description": "A library for adding worlds at runtime",
  "version": "${version}",
  "environment": "*",
  "depends": {
    "fabricloader": ">=0.15",
    "minecraft": ">=1.21"
  },
  "entrypoints": {
    "main": [
      {
        "adapter": "kotlin",
        "value": "dev.andante.bubble.Bubble"
      }
    ]
  },
  "accessWidener": "bubble.accesswidener",
  "mixins": [ "bubble.mixins.json" ]
}
